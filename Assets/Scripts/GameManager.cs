using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameManager : MonoBehaviour
{
    // Todo deprem gerçeği
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("Edleron Git repos");
    }

    // Update is called once per frame
    void Update()
    {

    }
}
